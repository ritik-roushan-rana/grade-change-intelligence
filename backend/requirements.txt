# Grade Change Intelligence — API dependencies.
#
# The ML stack is pinned to the same versions the models were developed and
# validated against (mirrors the root requirements.txt), so predictions from the
# API match the Streamlit dashboard exactly. Only the web layer is new.

# --- Web layer ---
fastapi==0.121.2
uvicorn[standard]==0.40.0

# --- ML / data stack (unchanged from the validated dashboard) ---
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0

# --- Startup ---
# Pulled in by scikit-learn, pinned here because the startup path imports it
# directly to load the models trained at build time.
joblib==1.5.3
