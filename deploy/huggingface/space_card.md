---
title: Grade Change Intelligence
emoji: 🏭
colorFrom: blue
colorTo: gray
sdk: docker
app_port: 8000
pinned: false
short_description: QCS assistant that predicts off-spec paper during grade changes
---

<!--
This block is the Space card. `deploy/huggingface/deploy.sh` prepends it to the
project README when it pushes, because Spaces reads its hardware and port
configuration from the README front matter. `app_port` must match the port the
container listens on (the Dockerfile defaults to 8000).
-->
